# client/.env.example
# ─────────────────────────────────────────────────────────────────────────────
# Copy to client/.env and fill in values.
# All VITE_* vars are bundled into the client build — NEVER put secrets here.
# ─────────────────────────────────────────────────────────────────────────────

# Backend API base URL (without trailing slash)
# Dev default: Vite proxy forwards /api → http://localhost:3000/api
# Production : set to your deployed backend, e.g. https://api.yourdomain.com
VITE_API_URL=/api

# ─── Web3 / MetaMask integration (optional) ──────────────────────────────────
# Leave blank (or omit) to disable on-chain logging entirely.
# The app is fully functional without these values — web3 calls become no-ops.
#
# VITE_WORKFORCE_LOGGER_ADDRESS
#   The deployed address of WorkforceLogger.sol.
#   Printed by `npm run deploy:amoy` (or any deploy script) as:
#     WORKFORCE_LOGGER_ADDRESS=0x...
#   Copy that value here.
VITE_WORKFORCE_LOGGER_ADDRESS=
