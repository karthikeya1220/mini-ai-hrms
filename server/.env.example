# ─── Database ───────────────────────────────────────────────────────────────
DATABASE_URL="postgresql://neondb_owner:npg_kK5eW3Ftyaxn@ep-dark-hat-aisn3669-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# ─── JWT ────────────────────────────────────────────────────────────────────
# Use a long random string (e.g. openssl rand -base64 64)
JWT_SECRET="rCUuS33y05Iz9i/Xa73NXsADxGa83jY7DALjRrYj5AWdK3nzBmx7P00r/zdoH2cbcHNdvIplHMgTghMzOMiIMjQ=="
JWT_REFRESH_SECRET="Ns/vNYonKGmcRXQzJq2EqZ6ws77Z8LG5vt5j0KPkojcoE88nYJtCL+vCTAsQrg7gfEh9+zNCI9rKfXPnUzeb7g=="
JWT_EXPIRES_IN="1h"
JWT_REFRESH_EXPIRES_IN="7d"

# ─── Server ─────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV="development"

# ─── CORS ───────────────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins
ALLOWED_ORIGINS="http://localhost:5173"

# ─── Redis (optional — caching) ─────────────────────────────────────────────
# When set, GET /api/dashboard results are cached for 60 s per org.
# The cache is invalidated automatically on every task status update.
# If unset, the API falls back to a live DB query on every request.
#
# Local dev:           redis://localhost:6379
# Cloud (Upstash TLS): rediss://:token@host.upstash.io:6379
# Cloud (Railway):     redis://default:password@containers-us-west-1.railway.app:6379
REDIS_URL=

# ─── Blockchain (Web3) ──────────────────────────────────────────────────────
# JSON-RPC endpoint for the network where WorkforceLogger is deployed.
# Polygon Amoy testnet (recommended for dev):
#   https://rpc-amoy.polygon.technology
# Alchemy / Infura also accepted:
#   https://polygon-amoy.g.alchemy.com/v2/<key>
WEB3_RPC_URL="https://rpc-amoy.polygon.technology"

# Address printed by `npm run deploy:amoy` — copy the WORKFORCE_LOGGER_ADDRESS line.
WORKFORCE_LOGGER_ADDRESS="0x_replace_with_deployed_contract_address"

# WARNING: Never commit a real private key. Use a throwaway testnet wallet only.
# Generate one: `node -e "const {Wallet}=require('ethers');console.log(Wallet.createRandom().privateKey)"`
DEPLOYER_PRIVATE_KEY="replace-with-testnet-wallet-private-key"

# ─── Gemini AI ───────────────────────────────────────────────────────────────
# Get your key at: https://aistudio.google.com/app/apikey
# Used by POST /api/ai/analyze (score analysis, skill gap analysis, workload prediction).
# If unset, the endpoint returns a deterministic fallback — no 500 errors.
GEMINI_API_KEY="your-gemini-api-key-here"
