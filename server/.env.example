# ─── Database ───────────────────────────────────────────────────────────────
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/rizeos_db"

# ─── JWT ────────────────────────────────────────────────────────────────────
# Use a long random string (e.g. openssl rand -base64 64)
JWT_SECRET="replace-with-a-very-long-random-secret"
JWT_REFRESH_SECRET="replace-with-a-different-very-long-random-secret"
JWT_EXPIRES_IN="1h"
JWT_REFRESH_EXPIRES_IN="7d"

# ─── Server ─────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV="development"

# ─── CORS ───────────────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins
ALLOWED_ORIGINS="http://localhost:5173"

# ─── Blockchain (Web3) ──────────────────────────────────────────────────────
POLYGON_RPC_URL="https://rpc-mumbai.maticvigil.com"
CONTRACT_ADDRESS="0x_replace_with_deployed_contract_address"
# WARNING: Never commit real private keys. Use a throwaway testnet wallet only.
PRIVATE_KEY="replace-with-testnet-wallet-private-key"
