// Services directory — stub.
// Services to be added:
//   auth.service.ts       — register / login / token refresh logic
//   employee.service.ts   — CRUD with org scoping
//   task.service.ts       — CRUD + status FSM
//   ai.service.ts         — productivity score, recommendations, skill gaps
//   web3.service.ts       — ethers.js + blockchain log storage
export { };
