# contracts/.env.example
# ─────────────────────────────────────────────────────────────────────────────
# Copy this file to contracts/.env and fill in your values.
# NEVER commit .env with real private keys — it is already in .gitignore.
# ─────────────────────────────────────────────────────────────────────────────

# ─── Deployer wallet ─────────────────────────────────────────────────────────
# 64-char hex private key of the wallet that deploys and calls registerOrg().
# For testnets: use a dedicated throw-away account with NO real funds.
# For mainnet : use a hardware wallet via frame.sh or ledger-live.
DEPLOYER_PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# ─── RPC endpoints ───────────────────────────────────────────────────────────
# Recommended providers (all offer generous free tiers):
#   Alchemy  : https://alchemy.com
#   Infura   : https://infura.io
#   Ankr     : https://www.ankr.com/rpc/  (no key required for public RPCs)
#   QuickNode: https://www.quicknode.com

# Polygon Mumbai ⚠ DEPRECATED (April 2024 — public RPCs are unreliable)
# Kept for legacy reference only. Use AMOY_RPC_URL for active development.
# MUMBAI_RPC_URL=https://polygon-mumbai.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Polygon Amoy ✅ ACTIVE testnet (chain 80002)
# Get a free key at https://alchemy.com or use the official rate-limited RPC.
# Official (rate-limited):  https://rpc-amoy.polygon.technology
# Alchemy:                  https://polygon-amoy.g.alchemy.com/v2/YOUR_KEY
# Infura:                   https://polygon-amoy.infura.io/v3/YOUR_KEY
AMOY_RPC_URL=https://rpc-amoy.polygon.technology

# Polygon mainnet (chain 137)
POLYGON_RPC_URL=https://polygon-rpc.com

# Ethereum Sepolia testnet (chain 11155111)
SEPOLIA_RPC_URL=https://rpc.ankr.com/eth_sepolia

# Ethereum mainnet (chain 1)
MAINNET_RPC_URL=https://rpc.ankr.com/eth

# ─── Block explorer API keys ─────────────────────────────────────────────────
# Required for `npx hardhat verify` (contract source verification).
# Get a free key at https://polygonscan.com/myapikey
POLYGONSCAN_API_KEY=YOUR_POLYGONSCAN_API_KEY

# Get a free key at https://etherscan.io/myapikey
ETHERSCAN_API_KEY=YOUR_ETHERSCAN_API_KEY

# ─── Deploy safety ───────────────────────────────────────────────────────────
# The deploy script blocks mainnet (chain 1, 137) by default.
# Set this to "true" to allow a mainnet deploy.
ALLOW_MAINNET_DEPLOY=false

# ─── Gas reporter ────────────────────────────────────────────────────────────
# Set to "true" to print a gas-usage table after `npm test`.
REPORT_GAS=false

# ─── Deployed contract address ───────────────────────────────────────────────
# Filled automatically by the deploy script output.
# Copy the printed value here, then copy it to server/.env as well.
WORKFORCE_LOGGER_ADDRESS=

# ─── Server integration ───────────────────────────────────────────────────────
# These go in server/.env (not here) — shown for documentation purposes.
# WEB3_RPC_URL=<same as AMOY_RPC_URL or POLYGON_RPC_URL>
# WORKFORCE_LOGGER_ADDRESS=<deployed contract address>
# DEPLOYER_PRIVATE_KEY=<same key, for signing logTaskCompletion calls>
